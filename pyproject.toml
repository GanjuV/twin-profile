[project]
name = "twin-profile"
version = "0.1.0"
description = "AI digital twin chatbot built with Gradio and Groq"
requires-python = ">=3.10"
dependencies = [
    "requests",
    "gradio",
    "pypdf",
    "openai",
    "python-dotenv",
]
